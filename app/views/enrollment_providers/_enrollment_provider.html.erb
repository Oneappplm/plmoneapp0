<% url = (action_name == 'show' ?  enrollment_providers_path : enrollment_provider_path(enrollment_provider))%>
<tr>
	<td scope="row">
    <%= link_to url, class: 'text-dark-grey' do %>
      <%= enrollment_provider.id %>
    <% end %>
  </td>
	<td scope="row">
      <%= link_to url, class: 'text-dark-grey' do %>
        <%= enrollment_provider.provider_name %>
      <% end %>
    </td>
	<td>
      <%= link_to url, class: 'text-dark-grey' do %>
        <%= enrollment_provider.npi %>
      <% end %>
  </td>
 <!-- <td>
      <%= link_to url, class: 'text-dark-grey' do %>
        <%= enrollment_provider.ssn %>
      <% end %>
  </td>
	 <td>
    <%#= link_to url, class: 'text-dark-grey' do %>
      <%#= enrollment_provider.start_date&.strftime("%m/%d/%Y") %>
    <%# end %>
  </td>
	<td>
    <%#= link_to url, class: 'text-dark-grey' do %>
      <%#= enrollment_provider.due_date&.strftime("%m/%d/%Y") %>
    <%# end %>
  </td> -->
	<td scope="row">
			<div class="d-flex gap-2">
				<%= link_to edit_enrollment_provider_path(enrollment_provider), class: 'text-dark-grey', class: 'btn btn-xs btn-primary' do %>
					<i class="bi bi-pencil"></i>
				<% end %>
        <%= button_to enrollment_provider_path(enrollment_provider), method: :delete, class: 'btn btn-xs btn-danger text-dark-grey delete-button', data: { confirm: 'Are you sure you want to delete this enrollment?' } do %>
          <i class="bi bi-trash"></i>
        <% end %>
			</div>
          <button id="backToTopBtn" class="btn-back-to-top"><i class="fa-solid fa-arrow-up"></i></button>
	</td>
</tr>