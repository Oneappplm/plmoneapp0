<nav>
        <div class="container">
            <div class="d-flex justify-content-end gap-2">
																<% if current_user.can_access?('Manage Provider') %>
                	<a href="<%= office_managers_path %>" class="btn btn-nav  <%= action_name == 'index' ? 'btn-primary active' : 'bg-alt-grey text-black' %>">Manage Providers</a>
																<% end %>

																<% if current_user.can_access?('Manage Practice') %>
                	<a href="<%= manage_practice_locations_office_managers_path %>" class="btn btn-nav <%= action_name == 'manage_practice_locations' ? 'btn-primary active' : 'bg-alt-grey text-black' %> ">Manage Practice</a>
																<% end %>
                <!-- <a href="<%= office_managers_path(page: 'office_manager_admin') %>" class="btn btn-nav <%= (params[:page] == 'office_manager_admin' or params[:page] == 'add_new_office_manager')  ? 'btn-primary active' : 'bg-alt-grey text-black' %>">Office Manager Admin</a> -->
            </div>
        </div>
</nav>