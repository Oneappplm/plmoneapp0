<div class="content container-fluid">
	<div class="row mb-4">
		<div class="col-lg-12 d-flex justify-content-end gap-2">
			<a href="javascript:void(0);" class="btn btn-md border-0 px-5 <%= enrollment_nav_active('users')%>">Users</a>
			<a href="<%= role_based_accesses_path %>" class="btn btn-md border-0 <%= enrollment_nav_active('role_based_accesses')%>">Role Access</a>
		</div>
	</div>
	<%= render 'user_restriction', locals: { access_key: 'Users' } do %>
		<%= render 'form' %>
	<% end %>
</div>

