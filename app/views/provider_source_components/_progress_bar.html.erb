<div class="col-lg-12 my-3">
					<div class="d-flex justify-content-between align-items-center gap-3">
						<div class="flex-grow-1">
							<div class="d-flex align-items-center">
								<div class="d-block progress-bar">
									<!-- percentage can be placd here as an inline css but if there's another way it's better. -->
									<div class="bg-primary" id="progress_bar_width" style="width: <%= percent %>%;"></div>
								</div>
							</div>
						</div>
						<span><small id="progress_bar_text"><%= percent %>%</small></span>
					</div>

</div>