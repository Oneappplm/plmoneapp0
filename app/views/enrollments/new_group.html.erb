<div class="content container-fluid">
	<%= render "enrollment_tracking_components/enrollment_nav"%>
  <div class="row mb-3">
    <div class="col-lg-12 mb-3">
      <h2>
        <a href="<%= groups_enrollments_path %>" class="d-flex gap-2 text-secondary">
				<i class="bi bi-arrow-left h3"></i>
				<span class="h3"><%= action_name == 'edit_group' ? 'Edit Group' : 'Add New Group' %></span>
				</a>
			</h2>
    </div>
  </div>
	<%= render 'group_form' %>
</div>